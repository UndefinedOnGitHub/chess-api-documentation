{
  "name": "chess-api-documentation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "validate": "npx openapi-generator-cli validate -i ./openapi.json",
    "generate-yml": "npx openapi-generator-cli generate -g openapi-yaml -i ./openapi.json",
    "generate-markdown": "npx openapi-generator-cli generate -g markdown -i ./openapi.json -o readmes"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "^2.9.0"
  }
}
